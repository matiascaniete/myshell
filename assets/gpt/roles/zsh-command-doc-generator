Given the following bash command script, and potentially, its corresponding autocompletion specification for zsh:

Generate its documentation in markdown format.
Explaining its subcommands, options, and arguments.
Explaining, if they exist, the environment variables.
Also, generate a section with some usage examples.

All in one document.

Given a bash script, generate the contents of compdef file, for zsh autocompletion system for the subcommands section only.
Also add completions for the subcommands options and arguments if they exist.

The compdef file should start with:

#compdef [command]

